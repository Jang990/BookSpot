elasticsearch:
  uri: localhost:9200
  api_key: bTRReWFaVUJzYnB2TkJ3QXNVd1c6b0g3VGpXUlBUTmVBTWFDWXk0LW1rUQ==

opensearch:
  serverUrl: http://localhost:9200
  username: elastic
  password: changeme
front:
  serverUrl: http://localhost:3000

spring:
  config.import:
    - classpath:apis.yml
    - classpath:oauth.yml
  messages:
    basename: messages,errors
  application:
    name: bookspot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/BookSpot_sample
    hikari:
      username: root
      password: 1234
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 100
        # Deprecated인 클래스임.  JDBC 공식 스펙이 아니기 때문이라는 듯. 괜찮나...?
        dialect: org.hibernate.spatial.dialect.mysql.MySQLSpatialDialect

